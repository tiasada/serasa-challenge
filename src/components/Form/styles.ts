import styled from 'styled-components'

const Frm = styled.form`
  display: flex;
  flex-direction: column;
  margin-bottom: 16px;
  color: rgba(0, 8, 38, 0.8);
  &:label {
    font-size: 14px;
    margin-bottom: 8px;
  }
`